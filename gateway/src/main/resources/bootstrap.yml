spring:
  application:
    name: iskander4j-bakend-gateway
  profiles:
    active: local
  config:
    import:
      optional:
        configserver: http://myconfigserver.com

---

spring:
  profiles: local